const setPinKey=async(pin:string)=>{
    // it is the user PIN , it is use to vrify and encrypt the decoy key
    // use advance hasing algorithm to hash the pin and store it in indexedDB
    const ky =await 
}